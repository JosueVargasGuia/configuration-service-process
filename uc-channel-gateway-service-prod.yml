name:
  host: gateway
management:
   endpoint:
      gateway:
         enabled: true
   endpoints:
      web:
         exposure:
            include: gateway
eureka:
  client:
    service-url:
      defaultZone: http://192.168.18.11:8761/eureka/
    fetch-registry: true
    register-with-eureka: true    
  instance:
    hostname: 10.0.2.15